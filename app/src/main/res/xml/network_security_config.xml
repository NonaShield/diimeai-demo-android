<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config — DiimeAI Demo
  Restricts cleartext traffic. All connections must use TLS.
  NonaShield backend uses TLS 1.3 (self-signed cert for demo, Let's Encrypt for prod).
-->
<network-security-config>
    <!-- Block all cleartext (HTTP) traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
      Allow cleartext ONLY for localhost (emulator testing against local backend).
      Remove in production.
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
