# Signing secrets — NEVER commit
.signing.env
*.keystore
*.jks
local.properties

# Google service account key
google-service-account.json
*.json

# Build outputs
build/
.gradle/
*.apk
*.aab

# Explicitly track the bundled vendor SDK AAR (CI-built, version-pinned)
!app/libs/*.aar

# IDE
.idea/
*.iml
.DS_Store
